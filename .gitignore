ollama/volume
ollama37/volume
open-webui/volume
webtop/volume
gitlab/volume
gitlab/.env
webtop/.env
mcp-atlassian/.env
monitoring/.env
monitoring/volume
monitoring/exporter-targets.yml
gpu-exporter/volume

# Per-host compose overrides. `docker compose up` merges docker-compose.yml with
# docker-compose.override.yml automatically, so host-specific values live here
# and never dirty a tracked file. Each stack commits an .example alongside.
docker-compose.override.yml
